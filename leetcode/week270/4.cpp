#include <bits/stdc++.h>

using namespace std;

using LL = long long;

template <typename T>
void pr(vector<T> x)
{
    for (auto a : x)
    {
        cout << a << " ";
    }
    cout << endl;
}
class Solution
{
public:
    vector<int> prev[100005];
    vector<int> next[100005];
    vector<vector<int>> validArrangement(vector<vector<int>> &pairs)
    {
        for (int i{}, _ = pairs.size(); i < _; ++i)
        {
            auto &p = pairs[i];
            
        }
    }
};

int main()
{
}